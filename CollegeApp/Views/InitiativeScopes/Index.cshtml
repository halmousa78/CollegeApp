@model IEnumerable<CollegeApp.Models.ImprovementIndicator.InitiativeScope>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InitiativeScopeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InitiativeScopeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InitiativeScopeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.InitiativeScopeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InitiativeScopeId })
        </td>
    </tr>
}

</table>
