@model IEnumerable<CollegeApp.Models.UserViewRole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Role.RoleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserView.UserViewName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Role.RoleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserView.UserViewName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserViewRoleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserViewRoleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserViewRoleId })
        </td>
    </tr>
}

</table>
